import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  root: {
    flexGrow: 1,
    padding: "12px",
  },
  img: {
    margin: "auto",
    display: "block",
    maxWidth: "80%",
  },
}));
